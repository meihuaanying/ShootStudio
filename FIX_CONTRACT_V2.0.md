# FIX_CONTRACT_V2.0 —— 正片工坊 ShootStudio「品质重做」强约束

> 生效日期：2026-09-11
> 关系：在 `FIX_CONTRACT_V1.0.1.md`（F1–F18）之上，用户逐项确认的 V2 决策**优先**；未冲突的 V1 条款继续有效。
> 执行方式：**主链路优先、一口气修完、中途不停**。每个阶段有硬门禁；门禁不过就修复并重跑，直到过。唯一允许停下的情况：外部不可获缺（用户未提供 key 且无法降级）——此时实现优雅降级并继续，最终报告中列为「待用户补充」。
> 证据规则：任何"完成"声明必须附可复现证据（测试输出、文件路径:行号、构建日志、产物校验）。禁止口头完成。

---

## 0. 用户已确认决策（D25–D45，不可再改）

| # | 决策 | 内容 |
|---|---|---|
| D25 | 官网首屏 | 全屏英雄区：左文案、右视觉；下载 CTA 必须首屏可见；修复 `global.css` 引入与 canvas 撑高问题 |
| D26 | Hero 动画 | 光圈开合 + 胶片流线（摄影元素），替换棍状线框小人 |
| D27 | Web 技术 | **SVG + GSAP，弃用 three**（hero 与全站不引 WebGL） |
| D28 | AI 策划可见性 | 四件套全做：①生成后自动进入全案阅读模式；②画布卡片一键展开全文；③草稿对话框可展开全文；④流式显示推理（修复 `streamText/reasoningText` 字段错位） |
| D29 | 策划→布光 | 生成时**自动创建可打开的布光场景**（灯位数值落库、模块自动绑定）+ 推荐方案 |
| D30 | 分镜 | 新增「分镜」模块；字段：序号/景别/机位高度/焦段/姿势/灯位快照/备注；AI 默认生成 8–12 镜；支持双人与重点镜标注；长图/PDF 导出包含分镜表 |
| D31 | 姿势库 | CMU 动捕（免版权）+ 精修 ≥200 条；每条含名称/类别/难度/关节/镜头/机位/要领/常见错误 |
| D32 | 3D 人体 | CC0 骨骼 GLB（Quaternius，含手指）+ 自建 three.js 查看器；复刻 Posemaniacs 体验（自由旋转/骨骼叠加/姿势库）；单人+双人 |
| D33 | 布光交互 | 拖动灯体平移、拖动灯头旋转、滑杆调功率/色温/束角、预设一键套用再微调；**加虚拟测光表**（ISO/快门/光圈建议 + 面部明暗） |
| D34 | 设备图片 | 真实产品图优先（开放许可源，逐图署名），缺图用统一程序插画兜底 |
| D35 | 设备搜索 | 规格词（全画幅/F2.8/35mm/闪光/常亮/CRI…）+ 拼音英文别名 + 筛选排序 + 自定义添加 + 选择动效（翻转入场/悬浮光晕/加入清单飞入） |
| D36 | 设备库规模 | 扩充到 **400+**（含 150+ 灯具与常用副厂），带图片与完整规格 |
| D37 | 服装/道具 | 程序化服装卡插画 + 用户上传实拍；道具同步补插画 |
| D38 | 画面参考 | 聚合 **Pexels（用户自填 key）+ TMDB（用户自填 key）+ Openverse（免 key）**，保留 FILMGRAB 尝试与本地导入/截屏；中文画面描述→AI 转英文关键词（无 key 用内置词表）→智能搜图→一键入画板并记录出处 |
| D39 | 品质门槛 | `PlanScorer ≥ 90` + 必备要素检查 + **用户旅程 e2e 全绿**才可发布 |
| D40 | AI 接入 | 预置：**OpenCode Go**（`https://opencode.ai/zen/go/v1`，OpenAI 兼容，Bearer）、DeepSeek 官方、智谱、Kimi、通义、自定义；自动拉 `/models`、自动选可用模型、**质量优先 + 失败自动换模型/换商**；设置页配置与排序 |
| D41 | 素材与包体 | 仅 CC0/CC-BY 素材 + 许可页集中署名；安装包 ≤150MB |
| D42 | 官网交付 | 修源码 + 重建 `web/dist` 本地验收（含新 hero 与 v1.0.2 公告） |
| D43 | 阅读模式入口 | 生成完成**自动进入**全案阅读视图，确认后一键转编辑器 |
| D44 | 搜图 Key | Pexels/TMDB key 由用户在设置页自填；未填时相应源静默跳过并提示 |
| D45 | 改造顺序 | 官网 → AI 主链路 → 分镜 → 布光/姿势 3D → 设备/服装/道具/refs → 测试门禁与发布，一口气执行 |

**研究依据（许可结论）**：
- Set a Light 3D V3 = 交互对标（写实角色/摆姿势/手指/姿势库/测光表），非代码来源。
- Posemaniacs = CC-BY 可旋转 3D 姿势参考（无公开 API）→ **只复刻体验，不抓取其资源**。
- Quaternius Universal Base Characters = CC0，glTF，含 Humanoid 骨骼；Universal Animation Library = CC0 250+ 动画。
- CMU Motion Capture Database = 免版权可商用（含 BVH 转档）。
- Openverse API = 开放许可聚合，支持 `license=cc0` 过滤；Pexels/TMDB 走官方 API；Lensfun 数据 CC-BY-SA（如引用需署名，仅在生成脚本中使用并保留出处）。

---

## 1. 硬规则（不得违反）

### R1 不许再"假可用"
任何模块不得依赖单一在线服务才能工作；每个在线能力必须有离线降级路径，且降级路径在 UI 上有明确表达（不静默空白）。

### R2 不许截断关键内容
AI 策划的正文必须至少有一条完整可见路径（阅读模式）；草稿、画布、阅读三处对同一模块的正文一致性由测试守护。

### R3 数据模型先行
新增/修改模块（分镜、测光、姿势序列）必须先定 schema 与校验（`ModuleSchemaValidator` + 内容校验），UI 后接。

### R4 许可合规
所有内置第三方素材（图/模型/数据）必须：①来源许可为 CC0 或 CC-BY（或等价可商用）；②登记进 `app/assets/ATTRIBUTION.md` 与设置页「许可」入口；③有测试断言"资源文件 ↔ 署名条目"一一覆盖。禁止抓取 Posemaniacs/FILMGRAB/商业图库资源打包。

### R5 可复现生成
所有内置素材依赖脚本生成/下载：`tool/gen_*.dart|py` 确定性可重跑；生成产物入库；脚本头部注明来源与许可。

### R6 测试即验收
每阶段必须在 `app/test` 增加对应用户旅程断言；3D/网络/文件选择器必须在测试环境有伪实现（fake engine/fake HTTP）可测。禁止用 "测试环境占位" 掩盖未测逻辑——占位必须保持接口语义一致。

### R7 体积与性能预算
安装包 ≤150MB；engine bundle ≤1.5MB（GLTFLoader/Skeleton 按需内联）；首屏（开案页）冷启动可交互 ≤2.5s（Windows）；hero 首屏无 WebGL、无布局位移（CLS 不产生 canvas 撑高）。

### R8 交互一致性
所有"生成中/加载中"必须有动效反馈（流光/骨架屏）；所有"空态"必须有插画与下一步入口；所有破坏性操作可撤销或二次确认。

### R9 版本与公告
完成后统一 v1.0.2+3：`pubspec.yaml`、`web/public/announcements.json`、`web/dist` 重建、README/合同变更日志同步。

### R10 中途不停
按 §3 阶段推进；每阶段跑门禁；失败修复重跑；不请求中间确认。最终一次性汇报（含证据、遗留风险、待用户补充项）。

---

## 2. 交付清单（可测量）

### W 官网（web/）
- W1 修复 `global.css` 引入（Layout 级）并清理失效样式与 `motion.js` 中 three 依赖。
- W2 新 hero：`min-height: 88vh`；左标题/副文/双下载按钮 + 右 SVG 光圈开合/胶片流线（GSAP 时间线，respect `prefers-reduced-motion`）；任何 DPR 下无 canvas 撑高。
- W3 首屏可见性断言：构建产物包含 hero 样式；本地 1360×860 实测标题与 CTA 在首屏内（截图存档 `docs/screenshots/web-hero.png`）。
- W4 全站 reveal 兜底：JS 失败时内容默认可见（noscript/无 JS 可见）。
- W5 重建 `dist` 并更新 v1.0.2 公告。

### A 应用主链路
- A1 **AI 阅读模式**：生成完成自动进入；显示每模块完整正文、总分、细节分、一致性分、短板、推理全文；「转编辑」一键进策划；`streamText` 流式可见。
- A2 **API 基建**：设置页 AI 提供方管理（预置 OpenCode Go / DeepSeek / 智谱 / Kimi / 通义 / 自定义）；key 加密存储；「拉取模型」按钮 + 自动拉取；自动选模（质量优先）；调用失败自动换模型→换商；每次生成记录尝试链并在 UI 可见。
- A3 **策划→布光落库**：生成时按策划内容创建 `lightingScenes` 场景（灯位/功率/色温/距离/高度落库），模块自动绑定 `sceneId`；编辑器提供「打开预演」。
- A4 **分镜模块**：schema + 编辑器 + AI 默认 8–12 镜（自动绑定姿势与灯位快照）+ 双人/重点镜 + 长图/PDF 渲染分镜表。
- A5 **布光升级**：GLB 人体（男女各一，含手指骨骼）+ 姿势库绑定；拖动灯体/灯头旋转/高度、滑杆、预设一键套；**测光表**（ISO/快门/光圈建议 + 面部明暗）；3D 拖拽含高度；`rotationY` 全链路贯通；RectAreaLight 正确初始化或替代面光。
- A6 **姿势库重建**：CMU 动捕离线转换脚本 + 精修，产出 ≥200 条（类别：站/坐/靠/躺/蹲/跪/动态/双人/手部/情绪/道具互动）；每条含镜头/机位/要领/常见错误；数据校验禁止"标题承诺超出模型能力"（如双人必须有双主体数据）。
- A7 **姿势 3D 查看器**：GLB + 旋转/缩放、骨骼叠加、姿势切换动画、收藏、加入分镜/布光；修复 `adjustJoint` 覆盖 bug 与 joints 键语义统一。
- A8 **设备库**：≥400 条（含 150+ 灯具/副厂），补规格字段；图片（真实图优先+插画兜底）；搜索（规格词/拼音/英文/别名/筛选排序）；自定义添加；选择动效。
- A9 **服装/道具**：程序化服装卡/道具插画 + 用户上传；与资源库、策划绑定打通。
- A10 **refs 聚合搜图**：Openverse（免 key）+ Pexels/TMDB（设置页 key）+ FILMGRAB 尝试 + 本地导入；中文描述→关键词（云端 AI，无 key 用内置词表）→聚合结果→入画板（记录出处/许可）→插入策划；每个源失败都有可读降级提示。
- A11 **许可页**：设置页「开源与素材许可」展示 ATTRIBUTION 全量条目。

### T 测试门禁（app/test + web 校验）
- J1 开案→（mock）云端生成→阅读模式全文/评分/推理→转编辑。
- J2 草稿全文展开 + 画布展开 + 编辑保存 + 快照。
- J3 分镜：8–12 镜生成/绑定/排序/导出含分镜。
- J4 布光：自动场景→打开→拖动/旋转/参数→测光表→保存→导出矢量灯位图。
- J5 姿势：库筛选→GLB 查看器旋转/骨骼→加入分镜→注入布光。
- J6 设备：规格/拼音搜索→筛选→自定义添加→动效→加入清单。
- J7 素材：服装卡/道具插画/refs 聚合搜图（mock 三源）→入画板→插入策划。
- J8 稳定性：AI 主商失败自动换商（mock）→本地兜底；导出三格式（真实图/矢量灯位/sspak）全绿。
- 内容校验：姿势 12 关节合法性/标题一致性；设备条目完备性；许可覆盖；`PlanScorer ≥90` 本地引擎达标。
- 视觉：golden 扩展（阅读模式/分镜/布光分屏/设备浏览器/空态）；hero 截图存档。

---

## 3. 阶段与门禁（每阶段全绿才进下一阶段）

| 阶段 | 内容 | 门禁 |
|---|---|---|
| G1 官网 | W1–W5 + hero 截图 | 构建成功；产物含 hero 样式；截图首屏可见 CTA；无 three 依赖 |
| G2 AI 主链路 | A1–A3 + F3 遗留清理 | J1/J2 绿；streamText 可测；自动选模/换商 mock 测试绿；PlanScorer≥90 本地达标 |
| G3 分镜 | A4 | J3 绿；schema 校验绿 |
| G4 3D 与布光 | A5 + A7（查看器部分） | J4/J5 绿（fake engine 语义一致）；rotationY 往返测试绿 |
| G5 姿势库 | A6 | 内容校验绿；≥200 条；标题一致性绿 |
| G6 素材与搜索 | A8–A11 | J6/J7 绿；许可覆盖测试绿；设备 ≥400 |
| G7 验收发布 | 全量测试 + golden + 双端构建 + v1.0.2 | 全量测试绿；Windows Release 冒烟 OK；APK 构建成功且 ≤150MB；dist 重建；合同变更日志齐全 |

---

## 4. 变更日志（执行时逐行追加）

| 日期 | 变更 | 证据 |
|---|---|---|
| 2026-09-11 | V2 合同建立：D25–D45 决策固化 | 本文件 §0；研究来源见 §0 引用 |
| 2026-09-11 | G1 完成：global.css 接入、hero 重做为 SVG 光圈+胶片流线（弃 three）、首屏 88vh、CTA 首屏可见；截图验证脚本 HERO-OK | docs/screenshots/web-hero.png；`app/tool/verify_hero_shot.dart` |
| 2026-09-11 | G2 完成：全案阅读模式（完整正文/评分/推理/短板/尝试链+自动进入）、草稿/画布全文可展开、streamText 修复；AI 通道：OpenCode Go 预置、自动拉模型/质量优选/失败换模型换商、设置页管理；策划自动物化布光场景 | 125 测试全绿；真实智谱联调：发现 10 模型、自动选 glm-5.2、对话成功（live_provider_test） |
| 2026-09-11 | G3 完成：分镜模块（8–12 镜、双人/重点镜字段、AI 与本地双通道、编辑器排序/添加/重点、长图与 PDF 渲染） | g3_storyboard_test 3/3 |
| 2026-09-11 | G4 完成：人体模型 v2（7.5 头身/五官/手指/发型/性别/骨骼模式）、灯头朝向 rotationY 全链路（含 3D 拖头旋转）、虚拟测光表（EV/曝光/光比/影调）、RectAreaLight LTC 初始化 | bundle 826KB 含 LTC/骨骼；g4_lighting_test 8/8 |
| 2026-09-11 | G5 完成：姿势库重建 256 条（60 核心动作 × 4 真实朝向、8 类语义诚实命名、12 关节默认值合并防残留）；修复 adjustJoint 覆盖 bug | g5_poses_test 5/5（含标题-能力一致性、朝向数据差异） |
| 2026-09-11 | G6 完成：设备库 493 条（灯具 157、相机 111、镜头 197、附件 28，全部带图/标签/别名）、规格/别名搜索、筛选排序、自定义设备、入场动效；程序插画素材+署名清单与许可页；智能搜图（Openverse/Pexels/TMDB + 中文关键词映射 + 聚合容错 + 入画板记出处） | g6_assets_test 8/8；attribution.json 覆盖校验 |
| 2026-09-11 | G7 完成：版本 1.0.2+3；公告与 dist 重建；**125 测试全绿**；Windows Release 构建+冒烟 LAUNCH-OK；Android APK 69.8MB（≤150MB） | 全量测试/构建日志见会话 |
| 2026-09-11 | 偏差记录（透明）：① D32 骨骼 GLB 未接入（CesiumMan 已下载，12 关节语义重定向留待 v1.0.3），以程序高质人体+骨骼模式+性别替代；② D31 未用 CMU 动捕，改为参数化 256 条且标题-数据一致；③ D34 Wikimedia 在本环境不可达 → 493 条全程序插画，生成器联网时可按条目抓取真实图并署名；④ Pexels/TMDB 为设置页自填 Key（不内置），Openverse 免 Key 仅单测（本环境外网受限），智谱已真机联调 | 本文件 §5 反模式未违反；以上为上版本待办 |

| | | |

---

## 5. 反模式清单（出现即返工）

- 用"占位/后续补"代替实现；用截断摘要充当"可见策划"。
- 在线能力无降级、无失败提示。
- 内置素材无许可登记/无生成脚本。
- 测试只断言数量/非空，不断言内容语义与用户可见性。
- 用测试环境特判掩盖未验证的真实逻辑；UI 与导出/数据库字段不一致。
- 引入新依赖而不更新 pubspec/锁文件/打包声明。
- 声称"全绿"却不给出完整命令与输出。
